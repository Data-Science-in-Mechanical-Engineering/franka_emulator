if(NOT TARGET Franka::@FRANKA_EMULATOR_NAME_UPPERCASE@)
	get_filename_component(FrankaEmulator_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
	include("${FrankaEmulator_CMAKE_DIR}/FrankaTargets.cmake")
	unset(FrankaEmulator_CMAKE_DIR)
endif()

set(@FRANKA_EMULATOR_NAME_UPPERCASE@_LIBRARIES Franka::@FRANKA_EMULATOR_NAME_UPPERCASE@)
get_target_property(@FRANKA_EMULATOR_NAME_UPPERCASE@_INCLUDE_DIRS Franka::@FRANKA_EMULATOR_NAME_UPPERCASE@ INTERFACE_INCLUDE_DIRECTORIES)

